// function myEach(arr, fn){
//     for(var i=0; i<arr.length;i++){
//         fn(arr[i], i, arr);
//     }
// }

// myEach([1,2,3], function(currentValue, index,array) {
//     console.log(currentValue, index, array);

// })
function merge(...arg) {
    if(arg.length >= 2) {
        var result = {};
        arg.forEach(function(element){
            Object.assign()
        })
    }else{

    }
}